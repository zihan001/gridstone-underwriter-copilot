# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# generated outputs (the pipeline writes these; not source)
out/
# NOTE: viewer/data.js is generated by the serializer. It is intentionally COMMITTED
# (checked-in snapshot for the sample subject so the viewer renders on a fresh clone
# without running the pipeline). Regenerate with `./scripts/run.sh`. If you'd rather it
# be build-only, uncomment:
# viewer/data.js

# env / secrets
.env
*.local

# editor / os
.DS_Store
.idea/
.vscode/
